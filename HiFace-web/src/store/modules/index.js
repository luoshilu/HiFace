import { makeface } from './makefaceModule'
import { headcp } from './headcpModule'
import { search } from './searchModule'

module.exports = {
  makeface: makeface,
  headcp: headcp,
  search: search
}
